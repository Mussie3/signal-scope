# 'local'  -- in-process simulation, no backend needed (default)
# 'remote' -- topology + live events from Supabase
VITE_FEED=remote

VITE_SUPABASE_URL=http://127.0.0.1:54331
VITE_SUPABASE_ANON_KEY=your-anon-key

# The public demo topology shipped by the migrations.
VITE_PROJECT_SLUG=demo
